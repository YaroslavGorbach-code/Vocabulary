package yaroslavgorbach.koropapps.vocabulary.feature.exercise.model

import yaroslavgorbach.koropapps.vocabulary.R

enum class ExerciseWordCategory(val resId: Int) {
    LETTERS(R.array.letters),
    FILLINGS(R.array.fillings),
    NOUNS(R.array.nouns),
    CATEGORY(R.array.categories),
}